version: 2
jobs:
  build:
    working_directory: ~/circleci-java-sbhw

    environment:
      MAVEN_OPTS: -Xmx3200m

    docker:
      - image: circleci/openjdk:8-jdk-stretch

    steps:

      - checkout

      - run:
          name: "CACHE_VERSION ?"
          command: echo ${CACHE_VERSION}

      # Download and cache dependencies
      - restore_cache:
          keys:
            - ${CACHE_VERSION}-dependencies-{{ checksum "pom.xml" }}
            # fallback to using the latest cache if no exact match is found
            - ${CACHE_VERSION}-dependencies-

      - run: mvn clean package # run the actual tests

      - save_cache: # saves the project dependencies
          paths:
            - ~/.m2
          key: ${CACHE_VERSION}-dependencies-{{ checksum "pom.xml" }}


      - store_test_results: # uploads the test metadata from the `target/surefire-reports` directory so that it can show up in the CircleCI dashboard.
          # Upload test results for display in Test Summary: https://circleci.com/docs/2.0/collect-test-data/
          path: target/surefire-reports
